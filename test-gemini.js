import { GoogleGenerativeAI } from "@google/generative-ai";
import fs from 'fs';

// Inicializar con alguna API Key o la que haya en el .env
// Pero como no tenemos la API Key aquí, tal vez no pueda probarlo directamente.
// Voy a buscar si hay una clave de Gemini en .env
